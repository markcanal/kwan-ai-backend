{
  "name": "kwan-ai-backend",
  "version": "0.1.0",
  "description": "Kwan-ai attendance & payroll backend (starter)",
  "main": "dist/main.js",
  "scripts": {
    "start": "node dist/main.js",
    "start:dev": "ts-node-dev --transpile-only --ignore-watch node_modules src/main.ts",
    "build": "tsc -p tsconfig.build.json",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init --preview-feature"
  },
  "dependencies": {
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0",
    "@nestjs/platform-express": "^10.0.0",
    "@nestjs/config": "^3.0.0",
    "reflect-metadata": "^0.1.13",
    "rxjs": "^7.8.0",
    "firebase-admin": "^12.0.0",
    "@prisma/client": "4.15.0",
    "prisma": "4.15.0",
    "bcrypt": "^5.1.0",
    "bullmq": "^1.84.0",
    "ioredis": "^5.3.2"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}